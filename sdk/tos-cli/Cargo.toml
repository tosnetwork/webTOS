[package]
name = "tos-cli"
version = "0.1.0"
edition = "2021"
description = "TOS CLI tools — build, deploy, replay, and inspect TOS agents"
license = "MIT"

[dependencies]
sha2 = "0.10"

[[bin]]
name = "tos"
path = "src/main.rs"
