[package]
name = "atos-wasm-sdk"
version = "0.1.0"
edition = "2021"
description = "ATOS WASM Agent SDK — write WASM agents for ATOS in Rust"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = "s"
lto = true
