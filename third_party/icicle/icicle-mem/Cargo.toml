[package]
name = "icicle-mem"
version = "0.3.0"
edition = "2021"
# Vendored from the icicle project; see third_party/icicle/LICENCE-MIT and -APACHE.
license = "MIT OR Apache-2.0"

[lib]
doctest = false

[dependencies]
tracing = { workspace = true }
ahash = { workspace = true }
