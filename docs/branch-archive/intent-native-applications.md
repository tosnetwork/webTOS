# `docs/intent-native-applications`

- Tip: `aea397c17a8b2ec276250ff5eb2236870cdb46f8`
- Merge base with archival `main`: `1004cd1c09aae847e91e946e874752ec6c187622`
- Relationship at archival: four commits behind `main`, three commits ahead

Unique commits:

1. `a2787cd` — `docs: describe intent-native dynamic applications`
2. `a3c2e96` — `docs: link intent-native application direction`
3. `aea397c` — `docs: position webTOS for intent-native applications`

The 571-line branch document is preserved as
[INTENT_NATIVE_APPLICATIONS.md](../INTENT_NATIVE_APPLICATIONS.md). The source
branch itself ends mid-sentence in section 9.1; the archive does not invent the
missing continuation. The root README positioning was independently committed
to `main` as `a1ad2ec` and was therefore not replayed.
